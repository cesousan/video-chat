{
  "name": "video_chat",
  "version": "1.0.0",
  "description": "a simple video chat app just for fun",
  "main": "index.js",
  "repository": "https://gitlab.com/cesousan/video-chat.git",
  "author": "cesousan & juanito",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"cd ./backend && yarn run dev\" \"cd ./ng-frontend && yarn run start\"",
    "dev-react": "concurrently \"cd ./backend && yarn run dev\" \"cd ./react-frontend && yarn run start\""
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  },
  "dependencies": {
    "@angular/animations": "^6.0.6",
    "@angular/cdk": "^6.3.1",
    "@angular/material": "^6.3.1",
    "rxjs": "^6.2.1",
    "yarn": "^1.7.0"
  }
}
